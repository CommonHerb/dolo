func open_gate(stock, need, locked):
  return stock > need and not locked
end

func restock_ready(stock, need):
  return stock < need or stock == need
end

func main():
  return (open_gate(7, 5, false), open_gate(2, 5, false), open_gate(7, 5, true), restock_ready(3, 5), restock_ready(5, 5))
end
