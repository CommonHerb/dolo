func alias_name(c):
  let alias = c
  return alias.0
end

func alias_hunger(c):
  let alias = c
  return alias.1
end

func main():
  let c = ("Ada", 3, 12)
  let alias = c
  return (alias_name(alias), alias_hunger(alias), alias.2, alias.1 > 2)
end
